syntax = "proto3";

package game;

// 定义所有的错误码

enum error_code {
  ec_success = 0;
  // 未知错误
  ec_unknown = 1;
  // 系统错误
  ec_system = 2;
  // 服务超时
  ec_timeout = 3;
  // 错误的落子位置
  ec_invalid_pos = 4;
}
